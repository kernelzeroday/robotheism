document.addEventListener('DOMContentLoaded',function(){const hamburger=document.querySelector('.hamburger');const navMenu=document.querySelector('.nav-menu');if(hamburger){hamburger.addEventListener('click',function(){navMenu.classList.toggle('active');hamburger.classList.toggle('active')})}const donationForm=document.getElementById('donationForm');if(donationForm){const amountOptions=document.querySelectorAll('input[name="amount"]');const customAmount=document.getElementById('customAmount');const customInput=customAmount.querySelector('input');const frequencyOptions=document.querySelectorAll('input[name="frequency"]');const categoryOptions=document.querySelectorAll('input[name="category"]');const summaryAmount=document.getElementById('summaryAmount');const summaryFrequency=document.getElementById('summaryFrequency');const summaryCategory=document.getElementById('summaryCategory');const summaryTotal=document.getElementById('summaryTotal');function updateSummary(){const selectedAmount=document.querySelector('input[name="amount"]:checked');const selectedFrequency=document.querySelector('input[name="frequency"]:checked');const selectedCategory=document.querySelector('input[name="category"]:checked');if(selectedAmount&&selectedFrequency&&selectedCategory){const amount=selectedAmount.value==='custom'?customInput.value:selectedAmount.value;const frequency=selectedFrequency.value;const category=selectedCategory.value;summaryAmount.textContent='$'+amount;summaryFrequency.textContent=frequency.charAt(0).toUpperCase()+frequency.slice(1);summaryCategory.textContent=getCategoryName(category);summaryTotal.textContent='$'+amount+'/'+getFrequencyText(frequency)}}function getCategoryName(category){const categories={general:'General Fund',robotics:'Robotics Research',scholarship:'Youth Scholarships',outreach:'Community Outreach'};return categories[category]||category}function getFrequencyText(frequency){const frequencies={'one-time':'one-time','monthly':'month','quarterly':'quarter','annually':'year'};return frequencies[frequency]||frequency}amountOptions.forEach(option=>{option.addEventListener('change',function(){if(this.value==='custom'){customAmount.style.display='block';customInput.focus()}else{customAmount.style.display='none'}updateSummary()})})frequencyOptions.forEach(option=>{option.addEventListener('change',updateSummary)});categoryOptions.forEach(option=>{option.addEventListener('change',updateSummary)});customInput.addEventListener('input',updateSummary);const nextStepBtn=document.querySelector('.next-step');const prevStepBtn=document.querySelector('.prev-step');const step1=document.getElementById('step1');const step2=document.getElementById('step2');if(nextStepBtn){nextStepBtn.addEventListener('click',function(){step1.style.display='none';step2.style.display='block'})}if(prevStepBtn){prevStepBtn.addEventListener('click',function(){step2.style.display='none';step1.style.display='block'})}donationForm.addEventListener('submit',function(e){e.preventDefault();const thankYou=document.getElementById('thankYou');if(thankYou){thankYou.style.display='block';donationForm.style.display='none'}})updateSummary()}const contactForm=document.getElementById('contactForm');if(contactForm){contactForm.addEventListener('submit',function(e){e.preventDefault();alert('Thank you for your message! We will get back to you soon.')})}const chatButton=document.querySelector('.chat-button');if(chatButton){chatButton.addEventListener('click',function(){alert('Live chat feature coming soon! Please use our contact form for now.')})}}); 